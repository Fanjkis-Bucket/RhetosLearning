using Rhetos.Compiler;
using Rhetos.Dsl;
using Rhetos.Extensibility;
using System.ComponentModel.Composition;

namespace BookStore.Service.RhetosEXT
{
    //[Export(typeof(IConceptCodeGenerator))]
    //[ExportMetadata(MefProvider.Implements, typeof(MonitoredRecordsInfo))]
    //public class MonitoredRecordsCodeGenerator : IConceptCodeGenerator
    //{
    //    public void GenerateCode(IConceptInfo conceptInfo, ICodeBuilder codeBuilder)
    //    {

    //        var info = (MonitoredRecordsInfo)conceptInfo;

    //        var code = String.Format(
    //        @"
                
    //        ",
    //      info.Entity.Module.Name,
    //      info.Entity.Name);
    //        throw new NotImplementedException();

    //        codeBuilder.InsertCode(code);
    //    }
    //}
}
